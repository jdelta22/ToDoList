[pytest]

DJANGO_SETTINGS_MODULE = config.settings

python_files = test_*.py tests_*.py *_tests.py tests.py

testpaths =
    tests

addopts =
    -v
    --tb=short

filterwarnings =
    ignore::DeprecationWarning